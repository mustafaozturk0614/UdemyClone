server:
  port: 9091

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

