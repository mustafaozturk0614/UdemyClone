server:
  port: 8090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://localhost:5858/userDb
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

